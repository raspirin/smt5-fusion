[package]
name = "smt5-fusion-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
serde = ["dep:serde"]

[dependencies]
num-bigint = "0.5.1"
serde = { version = "1.0.228", features = ["derive"], optional = true }
